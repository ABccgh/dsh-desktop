node_modules/
dist/
dist-installer/

# Generated by `npm run icon`: the outputs, plus the throwaway Chromium profile
# the rasteriser needs to capture a page. The icon is rebuilt from the installed
# DSH frontend favicon in seconds, so neither belongs in the repository.
build/

*.log
